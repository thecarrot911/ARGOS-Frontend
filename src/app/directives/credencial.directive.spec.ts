import { CredencialDirective } from './credencial.directive';

describe('CredencialDirective', () => {
  it('should create an instance', () => {
    const directive = new CredencialDirective();
    expect(directive).toBeTruthy();
  });
});
